#set "str" "Hello, world!";
print str;
#set "ptr" (to.float str);
:set ptr (+ ptr 3); 
#set "str2" (to.int ptr);
type.set str2 type.string;
print str2;
type.set str2 type.other;// if we dont do this the compiler will try to unallocate the (duplicated) pointer and crash;