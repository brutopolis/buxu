hash.set "title" "ELES";
hash.set "version" "0.0.1";

hash.set "world.creatures" (list.new);

hash.set "newCreature" (file.read (string.concat file.path "script/newCreature.br"));

do newCreature "joao";
mem.clear @-:-;
print joao;
return 123;