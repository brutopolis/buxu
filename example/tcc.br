hash.set "state" (tcc.new);
tcc.include state "./include/";
tcc.compile state 
(.
    #include "bruter.h"

    Int teste(VirtualMachine *vm, IntList* args)
    {
        for (int i = 0; i < 100000; i++)
        {
            printf("%d\n", i);
        }
        return -1;
    }
);
tcc.relocate state;
hash.set "teste.func" (tcc.get state "teste");
print teste.func;
teste.func;