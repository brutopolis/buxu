using math;
(set a(+(4)(55)(6)(-(^(4)(4))(%(45)(7)))));
print it is $a;


comment in this case help is run 3 times and then return 4 when it haves a return the function stops;
(help;help;help;? `return(4)`);
comment in this case help runs only the first time and then return 4, so the function is interrupted all the other commands after the first return are not executed;
(help; ? `return(4)`;help;help;help;? `return(12)`);