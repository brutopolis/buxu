reserved

(expression) = expression
!(string) = string
@key = variable
@number = get a variable from pointer(int)
@(expression) = use expression result as key for search a variable
@!(string) = use literal string as key for search a variable
@@key = get a variable key from @key varaible value

(list) = list


make = create a empty object outside the vm and return a pointer to it
new = create a variable in the vm and return it index
spawn = create a variable in the vm, set a hash and return it index
create = create a variable in the vm and return a pointer to it


Variable*   makeT       ();
int         newT        (VM vm, T value);
int         spawnT      (VM vm, char* hashkey, T value);
Variable*   createT     (VM vm, T value);